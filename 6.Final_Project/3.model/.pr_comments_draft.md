## PR 리뷰 코멘트 초안 — PR #145 (업데이트)

### 파일: `app/services/vectordb_service.py`
- **L242, L295, L503**: [좋음] `collection.add()` → `collection.upsert()` 전환. 이력서 재업로드, 면접 데이터 재임베딩 시 중복 ID 에러 방지.
- **L310**: [좋음] `max_distance` 파라미터 추가. 기본값 `None`으로 하위 호환 유지. 호출자가 선택적으로 사용.
- **L345-347**: [좋음] distance threshold 초과 시 `continue`로 스킵. 저품질 결과가 RAG 컨텍스트에 유입 방지.
- **L355-368**: [정보] 로깅을 f-string에서 `%s`/`%d`/`%.2f` 포맷으로 전환. Log Injection 방지 패턴 준수.

### 파일: `app/domain/chat/chains.py`
- **L190**: [좋음] `search_type="similarity_score_threshold"` 적용. `score_threshold=0.3`으로 유사도 30% 미만 문서 반환 차단.
- **L190**: [제안] `score_threshold` 값을 환경변수로 분리하면 환경별 튜닝 가능. 현재 값(0.3)은 적절.

### 전체 코멘트 (PR 본문용)
- `upsert` 전환 + distance 필터 + score threshold 세 가지가 모두 방어적 설계.
- 모든 변경이 하위 호환 유지 — 기존 호출 코드 수정 불필요.
- CI 전체 통과 (Lint, Format, Import, 보안).
