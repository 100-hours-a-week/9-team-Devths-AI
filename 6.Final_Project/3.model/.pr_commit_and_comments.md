# 커밋 · PR · PR 코멘트 초안

## 1. CI 사전 검증 결과 (3.model)

| 항목        | 결과 | 비고 |
|------------|------|------|
| Ruff Lint  | SKIP | 프로젝트 Python 3.10 요구, 로컬 3.13으로 poetry 실행 불가 |
| Ruff Format| SKIP | 동일 |
| Import 검증| SKIP | 동일 |
| 보안 패턴  | PASS | 변경 파일 내 로그/시크릿/인젝션 패턴 없음 |

**안내**: 커밋 후 CI(GitHub Actions)에서 Ruff·Import 검증이 수행됩니다. 로컬에서 검증하려면 `poetry env use python3.10` 후 `poetry run ruff check app/` 등을 실행하세요.

---

## 2. 커밋 메시지 초안

```
feat: RAG 앙상블 리트리버 도입 (ADR-069, BM25+MMR RRF)
```

---

## 3. PR 제목 (70자 이내)

```
feat: RAG 앙상블 리트리버 도입 (ADR-069, BM25+MMR RRF)
```

---

## 4. PR 본문 초안

```markdown
## 작업한 내용
- ADR-069 Option 1 반영: 희소(BM25) + 밀집(MMR) 앙상블, RRF 병합, 재정렬(rag_rerank_top_k) 적용
- `app/config/settings.py`: rag_use_bm25, rag_ensemble_dense_weight, rag_ensemble_sparse_weight, rag_rerank_top_k 설정 추가
- `app/domain/chat/chains.py`: retrieve_context_documents(), _format_pairs_to_context() 추가, retrieve_context() 리팩터
- `app/services/rag_service.py`: BM25Retriever·RRF 병합·앙상블 분기 추가, rag_use_bm25 시 dense+sparse 결합 후 포맷
- `pyproject.toml`: rank-bm25 의존성 추가

## 참고 사항
- rag_use_bm25=False 또는 vectordb 없으면 기존 MMR/fallback 경로 유지
- 코드 리뷰 권장 사항(앙상블 try/except fallback, get_running_loop, BM25 문서 상한)은 추후 이슈/PR로 반영 가능

## 테스트 계획
- [ ] Ruff lint 통과 (`poetry run ruff check app/`)
- [ ] Ruff format 통과 (`poetry run ruff format --check app/`)
- [ ] Python import 검증 통과
- [ ] RAG 채팅 시 앙상블 경로 동작 확인 (rag_use_bm25=True)
```

---

## 5. PR 리뷰 코멘트 초안

### 파일: `app/config/settings.py`
- **L72~87**: [Good] ADR-069 설정이 한 곳에 모여 있어 운영·실험에 유리합니다.

### 파일: `app/domain/chat/chains.py`
- **L148~195**: [Good] retrieve_context_documents로 (collection_type, Document) 반환해 앙상블에서 재사용하기 좋습니다.
- **L197~214**: [Good] _format_pairs_to_context로 포맷 로직 분리되어 중복이 없습니다.

### 파일: `app/services/rag_service.py`
- **L81~127**: [Good] _rrf_merge 단일 책임, top_k 적용이 명확합니다.
- **L289~341**: [제안] 앙상블 블록에 try/except를 넣고, 실패 시 `return await self._rag_chain.retrieve_context(...)` 로 dense-only fallback 후 logger.warning 하면 안정성이 올라갑니다.
- **L320**: [주의] `asyncio.get_event_loop()` 대신 `asyncio.get_running_loop()` 사용을 권장합니다 (비동기 컨텍스트).
- **L302~316**: [제안] 사용자 문서가 매우 많을 때 메모리·지연을 위해 bm25_docs 상한(설정 또는 500~1000) 적용을 검토해 보세요.

### 파일: `pyproject.toml`
- **L71**: [Good] rank-bm25 버전 고정으로 재현성 확보됩니다.

### 전체 코멘트 (PR 본문용)
- ADR-069 앙상블·RRF·재정렬 반영이 잘 되어 있습니다. 앙상블 실패 시 dense-only fallback과 get_running_loop 변경을 다음 PR에서 반영하면 좋겠습니다.
```

---

## 6. 제출 방법 안내

- **커밋**: 3.model 디렉터리에서 `git add` 대상 4개 파일 스테이징 후 위 커밋 메시지로 커밋.
- **푸시**: `git push -u origin feature/yaml-prompt-management` (보호 브랜치 아님).
- **PR 생성**: `gh pr create --base develop --title "feat: RAG 앙상블 리트리버 도입 (ADR-069, BM25+MMR RRF)" --body-file .pr_body.md` 또는 GitHub 웹에서 Compare & pull request.
- **리뷰 코멘트**: 위 초안을 GitHub PR의 Review → Comment에 인라인/일반 코멘트로 붙여 넣거나, `gh pr review <PR번호> --comment -b "..."` 로 제출.
