# PR 리뷰 코멘트 초안 (PR #149)

## 파일: `app/config/settings.py`
- **L89~101**: [Good] ADR-069 후속 설정이 한 곳에 모여 있어 운영·실험에 유리합니다.
- **L95**: [참고] `rag_use_compressor` 기본 False로 비용·지연 부담 없음.

## 파일: `app/services/rag_service.py`
- **L23~27**: [Good] LLMChainExtractor 선택 import로 의존성 없을 때도 동작 보장.
- **L130~134**: [Good] `_rerank()`로 재정렬 단계가 명확히 분리됨.
- **L137~155**: [Good] `_compress_pairs_with_llm`에서 top_k로 압축 대상 제한, 실패 시 비압축 반환.
- **L153**: [제안] 로그에 예외 전체 대신 `type(e).__name__` 또는 짧은 메시지만 남기면 로그 과다·민감 정보 노출을 줄일 수 있습니다.
- **L312~318**: [Good] `dense_weight`/`sparse_weight`로 invoke 시점 가중치 오버라이드, A/B 테스트에 활용 가능.
- **L358~360**: [Good] BM25 인덱스 상한으로 메모리·지연 완화.
- **L364**: [Good] `get_running_loop()` 사용으로 비동기 컨텍스트 적절.
- **L383~388**: [Good] RRF 후 재정렬 단계를 명시한 흐름.
- **L390~397**: [Good] 압축기는 설정·top_k로 제어됨.
- **L401~404**: [제안] fallback 로그에 `type(e).__name__`만 남기면 충분할 수 있습니다.

## 전체 코멘트 (PR 본문용)
- ADR-069 후속 반영이 잘 되어 있습니다. BM25 상한, 재정렬 단계, invoke 시 가중치, 압축기 경로가 명확합니다.
- 앙상블 실패 시 fallback과 `get_running_loop()` 반영이 좋습니다. 로그에는 예외 타입만 남기는 것을 권장합니다.
- 제출: `gh pr review 149 --comment -b "위 전체 코멘트"` 또는 인라인은 GitHub PR 화면에서 해당 라인에 코멘트로 붙여 넣으면 됩니다.
