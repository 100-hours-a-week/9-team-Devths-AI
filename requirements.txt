# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Data validation
pydantic>=2.12.0
pydantic[email]>=2.12.0

# File handling
python-multipart==0.0.6

# HTTP client
httpx==0.26.0

# Image processing
Pillow==10.2.0

# PDF processing
pdf2image==1.17.0
pdfplumber==0.11.0
poppler-utils  # Required for pdf2image (system dependency)

# OCR (EasyOCR만 사용 - 02_OCR_모델_선정.md 참고)
easyocr==1.7.0

# HuggingFace Gradio Client (optional, for fallback)
# gradio-client==0.10.1

# Google Gemini API (required for face detection)
google-genai

# OpenCV for face detection (fallback)
opencv-python>=4.8.0

# Microsoft Presidio (for text PII detection)
presidio-analyzer
presidio-image-redactor
spacy>=3.0.0

# Chandra OCR model (datalab-to/chandra)
transformers>=4.30.0
torch>=2.0.0
git+https://github.com/datalab-to/chandra.git

# RAG and VectorDB (required for chatbot)
chromadb==0.4.24
numpy>=1.24.0,<2.0.0  # ChromaDB 0.4.24 requires NumPy 1.x

# Hugging Face datasets
datasets>=2.14.0

# Optional for advanced features
# langchain==0.1.0  # RAG framework
# langchain-google-genai==0.0.6  # Gemini integration
# langgraph==0.0.20  # Agent framework
# paddleocr==2.7.0  # Advanced OCR

# Environment variables
python-dotenv>=1.1.1

# Langfuse for LLM observability
langfuse>=3.12.0
openai>=2.15.0
